# 比赛海报生成器

运行 `npm run lab:poster:reset` 后，实现 `workspace/core.js`，再完成 Canvas 绘制与 PNG 导出。

```bash
npm run lab:poster:test
```

预览：`http://127.0.0.1:5176/labs/match-poster/workspace/`

参考答案：`npm run lab:poster:solution`
